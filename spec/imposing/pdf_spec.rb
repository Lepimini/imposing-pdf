require "spec_helper"

RSpec.describe Imposing::Pdf do
  it "has a version number" do
    expect(Imposing::Pdf::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
